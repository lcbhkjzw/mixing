bfc的布局规则：
1、内部的block在垂直方向上一个接一个的放置
2、block之间垂直方向的距离由margin决定，相邻的block会折叠
3、每个元素的marginbox的左边，与包含块borderbox的左边接触。浮动也是如此。
4、bfc的区域不会和浮动兄弟元素重叠
5、内部独立
6、计算bfc的高度时，浮动元素也参与计算

产生bfc
1、根元素
2、设置了浮动
3、position为绝对定位
4、display为inline-block，table-cell，tabel-caption,flex,inline-flex
5、overflow不为visible


